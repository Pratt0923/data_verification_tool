<!-- Footer -->
<footer>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <div class="container">
        <div class="row">
            <div class="col-lg-12 text-center">
                <% if current_page?(root_path) %>
                    <%= link_to "Email", emails_path, class: "btn btn-danger btn-lg" %>
                    <%= link_to "Direct Mail", directmails_path, class: "btn btn-primary btn-lg"%>
                <% else %>
                    <%= link_to "Previous", :back, class: "btn btn-secondary btn-lg" %>
                    <%= link_to "Next", class: "btn btn-secondary btn-lg" %>
                <% end %>
            </div>
        </div>
    </div>
</footer>
